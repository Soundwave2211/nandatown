# SPDX-License-Identifier: Apache-2.0
[project]
name = "nanda-academy"
version = "0.1.0"
description = "Proof-driven agent academy, benchmark lab, certification authority, and blueprint factory for NANDA Town"
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["nanda_academy"]

